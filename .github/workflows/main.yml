name: MSBuild Main Workflow

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    uses: your-org-or-repo/.github/workflows/msbuild-reusable.yml@main
    with:
      platform: "Any CPU"
      configuration: "Release"
      maxCpu: 4
